apiVersion: tekton.dev/v1alpha1
kind: Task
metadata:
  name: azure-deploy-app
  namespace: azure-functionapp
spec:
  inputs:
    resources:
      - name: functionapp-git
        type: git
  steps:
    - name: install
      image: node:10
      workingDir: /workspace/functionapp-git/app
      command:
      - npm
      args:
      - install
      - --only=prod
    - name: deploy
      image: jmcshane/azure-func-cli:latest
      workingDir: /workspace/functionapp-git
      command:
      - sh
      args:
      - deploy.sh
      env:
      - name: USERNAME
        valueFrom:
          secretKeyRef:
            name: azure-credentials
            key: username
      - name: PASSWORD
        valueFrom:
          secretKeyRef:
            name: azure-credentials
            key: password
      - name: TENANT
        valueFrom:
          secretKeyRef:
            name: azure-credentials
            key: tenant
